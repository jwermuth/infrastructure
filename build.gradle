apply plugin: 'idea'
apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    testCompile(
            // Spock test framework
            'junit:junit:4.12',
            'org.codehaus.groovy:groovy-all:2.4.4',
            'org.spockframework:spock-core:1.0-groovy-2.4',
            // Test DNS is set up correctly by querying DNS server for info
            'dnsjava:dnsjava:2.1.1'
    )
    compile(
            // Wellll logging 8)
            'log4j:log4j:1.2.17',
            // Get rid of annoying error message
            'org.slf4j:slf4j-nop:1.7.21',
            // Wellll logging 8)
            'log4j:log4j:1.2.17',
            // Get rid of annoying error message
            'org.slf4j:slf4j-nop:1.7.21',
    )
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}
